module main;

func add(a: int, b: int) -> int {
    return a + b;
}

func main() -> int {
    let sum = 0;
    for (let i = 0; i < 3; i = i + 1) {
        if (i < 2) {
            sum = sum + add(i, 1);
        }
    }
    while (sum > 0) {
        return sum;
    }
    return 0;
}
