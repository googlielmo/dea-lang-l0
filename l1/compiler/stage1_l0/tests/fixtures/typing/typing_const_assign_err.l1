/*
 * SPDX-License-Identifier: MIT OR Apache-2.0
 * Copyright (c) 2026 gwz
 */

module typing_const_assign_err;

struct Point {
    x: int;
    y: int;
}

const answer: int = 1;
const origin: Point = Point(0, 0);

func err() -> int {
    answer = 2;
    origin.x = 3;
    return 0;
}
