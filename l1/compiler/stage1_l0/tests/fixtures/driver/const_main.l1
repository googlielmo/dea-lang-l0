/*
 * SPDX-License-Identifier: MIT OR Apache-2.0
 * Copyright (c) 2026 gwz
 */

module const_main;

struct Pair {
    left: int;
    right: int;
}

const pair: Pair = Pair(1, 2);
const answer: int = 20;

func main() -> int {
    return answer + pair.left + pair.right;
}
