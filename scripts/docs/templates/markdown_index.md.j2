# Dea/L0 API Markdown Index

Generated API reference index for the Dea/L0 codebase.

This index is built from Doxygen XML generated from source comments and docstrings for:

- Stage 1 Python compiler sources under `compiler/stage1_py/`
- Stage 2 L0 compiler sources under `compiler/stage2_l0/src/`
- Shared L0 standard library modules under `compiler/shared/l0/stdlib/`
- Shared runtime headers under `compiler/shared/runtime/`

Design and reference documents are located in the repository's `docs` directory and are not duplicated in this
index.

## About

- Project: Dea/L0, a small safe C-family systems language that compiles to C99.
- Source repository: [github.com/googlielmo/dea-lang-l0](https://github.com/googlielmo/dea-lang-l0).
- Created and maintained by `gwz` ([\@googlielmo](https://github.com/googlielmo)).

## Overview

This API reference supports both source-first and symbol-first browsing.
Source-set hubs provide broad context, source pages and dedicated type pages provide detail.

## Stage 1
Count: {{ stage1|length }} files
{% for page in stage1 %}
- [`{{ page.label }}`]({{ page.href }})
{% endfor %}

## Stage 2
Count: {{ stage2|length }} files
{% for page in stage2 %}
- [`{{ page.label }}`]({{ page.href }})
{% endfor %}

## Shared
Count: {{ shared|length }} files
{% for page in shared %}
- [`{{ page.label }}`]({{ page.href }})
{% endfor %}
